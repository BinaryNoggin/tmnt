# Tmnt.run()

# start a shell
# quit a shell (ctrl - c)
# add context (numbers on prompt)
# really quit (exit command)
# exit command goes to iex
# command results - :ok, and :exit
# security
# make exit command logout
# whoami
# autocomplete - slide with the way that expand_fun returns values
# inserting shared letters to be inserted is an exercise for the reader
# timeout
# commands that take arguments - countdown - OptionParser
# IO.ANSI
# register new commands
# otp behaviour
